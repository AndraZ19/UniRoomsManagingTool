@model IEnumerable<WebApplication6.Models.Curs>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nume)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Capacitate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PCReq)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VideoReq)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nume)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Capacitate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PCReq)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VideoReq)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CursId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CursId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CursId })
        </td>
    </tr>
}

</table>
